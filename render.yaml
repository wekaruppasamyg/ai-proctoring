services:
  - type: web
    name: ai-proctoring
    env: python
    pythonVersion: 3.11.6
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: DATABASE_URL
        scope: RUN
